<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1.0"/>
  <title>Forgot Password | ShramSetu</title>

  <script src="https://cdn.tailwindcss.com"></script>
  <link href="https://fonts.googleapis.com/css2?family=Inter:wght@400;600;700&display=swap" rel="stylesheet">

  <style>
    body {
      font-family: 'Inter', sans-serif;
      background: radial-gradient(circle at 20% 30%, #e0f2fe 0%, #f5f9ff 100%);
    }
    .glass {
      background: rgba(255, 255, 255, 0.9);
      backdrop-filter: blur(12px);
      -webkit-backdrop-filter: blur(12px);
    }
  </style>
</head>

<body class="min-h-screen flex items-center justify-center px-4">

  <div class="max-w-md w-full glass shadow-xl rounded-2xl p-8 space-y-6 border border-blue-100">

    <!-- Header -->
    <div class="text-center">
      <h2 class="text-3xl font-extrabold text-gray-800">Reset Your Password</h2>
      <p class="text-sm text-gray-500 mt-1">
        Enter your registered email and we'll send you a secure link to reset your password.
      </p>
    </div>

    <!-- Form -->
    <form method="POST" action="/forgetPassword" class="space-y-5">
      <div>
        <label for="email" class="block text-sm font-medium text-gray-700">Email address</label>
        <input 
          id="email"
          type="email"
          name="email"
          placeholder="example@email.com"
          required
          class="w-full mt-1 px-4 py-2 border border-gray-300 rounded-md shadow-sm focus:ring-2 focus:ring-blue-500 focus:outline-none"
        />
      </div>

      <button 
        type="submit" 
        class="w-full bg-blue-600 hover:bg-blue-700 text-white font-semibold py-2 rounded-md transition shadow"
      >
        Send Reset Link
      </button>
    </form>

    <!-- Additional Info -->
    <div class="text-sm text-gray-600 bg-blue-50 p-4 rounded-md border border-blue-200">
      <p><strong>Note:</strong> Reset link is valid for a limited time. Check spam folder if not found.</p>
      <p>Need help? Email us at <a href="mailto:support@shramsetu.in" class="text-blue-600 underline">shramsetu.team@gmail.com</a></p>
    </div>

    <!-- Footer -->
    <p class="text-center text-sm text-gray-500">
      Remember your password?
      <a href="/login_user" class="text-blue-600 hover:underline font-medium">Login</a>
    </p>

  </div>
</body>
</html>